require "spec_helper"

RSpec.describe Hyperlapse do
  it "has a version number" do
    expect(Hyperlapse::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
